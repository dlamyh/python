#######迭代dict
 d = {'a': 1, 'b': 2, 'c': 3}
>>> for key in d:
...     print(key)

默认情况下，dict迭代的是key。如果要迭代value，可以用for value in d.values()，如果要同时迭代key和value，可以用for k, v in d.items()

#######迭代字符
>>> for ch in 'ABC':

########迭代list
def findMinAndMax(L):
	max=L[0]
	min=L[0]
	for x in L:
		if x>max:
			max=x
		elif x<min:
			min=x
		else:
			pass
	return (max,min)
